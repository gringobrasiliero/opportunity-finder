

<%= render partial: 'form', locals: {identifier: @application.opportunity} %>
<%= link_to 'Delete',
             application_path(@application),
                         method: :delete,
                         data: { confirm: 'Are you sure?' } %>
